---
export interface Props {
  class?: string;
  vertical?: boolean;
}

const { class: className = '', vertical = false } = Astro.props;

const classes = vertical 
  ? `w-px h-full bg-border ${className}` 
  : `h-px w-full bg-border my-8 ${className}`;
---

<div class={classes}></div>